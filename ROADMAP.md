# 健身生活 RPG Roadmap

这个 roadmap 用来记录接下来一段时间的架构方向。项目仍然是个人本地工具，不引入登录、数据库或复杂后端。

## 近期结构调整

- [x] 将训练记录从单一“训练类型”拆成“训练大类”和“训练重点”。
- [x] 将晚饭记录从重复的“晚饭类型”改为“晚饭质量”和“晚饭标签”。
- [x] 把训练选项、晚饭选项集中到 `js/config.js`，方便以后扩展。
- [x] 在 `js/storage.js` 中兼容旧记录，旧的“上半身、下半身、拉面、炸物”等数据仍可读取。
- [x] 让 RPG 每日任务读取每日记录，不再要求上半部分额外手动打卡。
- [x] 让“今日训练”任务使用结构化训练重点。
- [x] 让推荐规则使用新的训练和晚饭字段。
- [x] 更新历史表格展示，让训练和晚饭信息更清楚。
- [x] 更新 README，说明新的记录模型。

## 下一阶段可做

- [ ] 增加数据导出和导入，方便备份 localStorage。
- [ ] 增加简单趋势图，例如体重、步数、训练次数。
- [ ] 给不同训练重点配置更具体的建议模板。
- [ ] 增加“身体状态”对推荐的影响，例如高疲劳或明显酸痛时降低训练强度。
- [ ] 增加成就徽章，但保持规则简单可读。

## 暂不做

- [ ] 登录、多用户和云同步。
- [ ] 数据库。
- [ ] React / Vue 重构。
- [ ] 复杂后端。
